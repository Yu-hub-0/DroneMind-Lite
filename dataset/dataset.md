# DroneMind-Lite Datasets

This directory contains dataset loading utilities for pretraining, supervised fine-tuning, preference optimization, and tool-use or agent-style training.

The project uses JSONL files with conversation-style records. UAV-domain sample data lives in `data/`, while dataset classes and masking logic live in this package.
